# media

这里放随插件分发的默认背景视频，命名为 `background.mp4`。插件在没有任何配置时
默认解析到这个文件；文件缺失时优雅降级（控制条显示 ⚠ 提示），不影响 DSH 使用。

**体积要求**：建议 < 20MB。GitHub 单文件上限 100MB，仓库里不要提交几百 MB 的原片。
背景视频本来静音，用 HandBrake / ffmpeg 压到 720p、1–2 Mbps 即可（45 秒视频约 6–12MB）。
